<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this destination from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
  
  <div class="col-md-12">
    <div class="field">
    <%= f.label :tilte %><br>
    <%= f.text_field :title, :size => "30" %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content, :size => '100' %>
  </div>
  
  
  
  <div class="actions">
    <%= f.submit "Save changes", class: "btn btn-primary" %>
  </div>
    
  </div>
  
</div>

  
<% end %>




