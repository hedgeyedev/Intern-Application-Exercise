<h1>Another Awesome Blog</h1>
<hr>
<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td class="posttitle"><%= post.title %></td>
      </tr>

      <tr>
        <td class="postcontent"><%= post.content %></td>
      </tr>

      <tr>
	<td></td>
 	<td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Remove', post, method: :delete, data: { confirm: 'Are you sure?' } %></td> 
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Submit New Post', new_post_path %>
